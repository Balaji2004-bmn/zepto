<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Remove: nav_back (back navigation doesn't belong in bottom nav) -->

    <item
        android:id="@+id/nav_home"
        android:icon="@drawable/baseline_home_24"
        android:title="Home" />

    <item
        android:id="@+id/nav_categories"
        android:icon="@drawable/baseline_category_24"
        android:title="Categories" />

    <!-- Remove: nav_apparel (this should be inside Categories) -->

    <item
        android:id="@+id/nav_cart"
        android:icon="@drawable/baseline_shopping_cart_24"
        android:title="Cart" />

    <!-- Change: Use better icon for orders -->
    <item
        android:id="@+id/nav_orders"
        android:title="Orders"
        android:icon="@drawable/baseline_receipt_24" />

    <!-- Change: Settings → Profile (more common) -->
    <item
        android:id="@+id/nav_profile"
        android:icon="@drawable/baseline_person_24"
        android:title="Profile" />

</menu>